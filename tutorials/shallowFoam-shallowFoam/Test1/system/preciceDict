FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "system";
    object      preciceDict;
}

preciceConfig "../precice-config.xml";

participant Test1;

modules (FF);

interfaces
{
  Interface1
  {
    mesh              Test1_Mesh;
    patches           (right);
    
    readData
    (
    );
    
    writeData
    (
      FlowDepth
      Discharges
    );
  };
};
