FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "system";
    object      preciceDict;
}

preciceConfig "../precice-config.xml";

participant rightSF;

modules (FF);

interfaces
{
  Interface1
  {
    mesh              right_Mesh;
    patches           (leftWall);
    
    readData
    (
    );
    
    writeData
    (
      Alpha
      Prgh
      Velocity
    );
  };
};
